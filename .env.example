# Arena Admin Password
# (Client-visible; keep this file for local dev only. For production, prefer server-side gating.)
NEXT_PUBLIC_ARENA_ADMIN_PASSWORD=your-secure-password-here

# Arena Backup Webhook URL (server-side only)
# Google Apps Script Web App URL (https://script.google.com/macros/s/.../exec)
# Used by `POST /api/arena/backup` as a proxy, so the webhook URL is never exposed to users.
ARENA_BACKUP_WEBHOOK_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
